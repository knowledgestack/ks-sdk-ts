/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * Caller's authorization on an item: whether an action is permitted.
 * 
 * Authorization only — not whether it will currently succeed. An authorized
 * write/delete can still be refused by transient state on the same response:
 * a run's ``execution_state`` (e.g. not IN_PROGRESS to delete), a
 * ``system_managed`` item, or a document that is ``approval_state``-sealed or
 * checked out by another user (see authorization.md §5.6). Ladder:
 * ``can_delete`` ⟹ ``can_write`` ⟹ ``can_read``; ``can_approve`` is orthogonal
 * and always false for agents.
 * @export
 * @interface ItemPermissions
 */
export interface ItemPermissions {
    /**
     * False only on a navigation-ancestor row (visible to traverse, not open).
     * @type {boolean}
     * @memberof ItemPermissions
     */
    canRead: boolean;
    /**
     * Authorized to create/update. Run: triggerer-or-admin, not path-based.
     * @type {boolean}
     * @memberof ItemPermissions
     */
    canWrite: boolean;
    /**
     * Authorized to delete. Run: triggerer-or-admin.
     * @type {boolean}
     * @memberof ItemPermissions
     */
    canDelete: boolean;
    /**
     * Authorized to approve/unapprove. Always false for agents (assumed).
     * @type {boolean}
     * @memberof ItemPermissions
     */
    canApprove: boolean;
}
export const ItemPermissionsPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the ItemPermissions interface.
 */
export function instanceOfItemPermissions(value: object): value is ItemPermissions {
    if (!('canRead' in value) || value['canRead'] === undefined) return false;
    if (!('canWrite' in value) || value['canWrite'] === undefined) return false;
    if (!('canDelete' in value) || value['canDelete'] === undefined) return false;
    if (!('canApprove' in value) || value['canApprove'] === undefined) return false;
    return true;
}

export function ItemPermissionsFromJSON(json: any): ItemPermissions {
    return ItemPermissionsFromJSONTyped(json, false);
}

export function ItemPermissionsFromJSONTyped(json: any, ignoreDiscriminator: boolean): ItemPermissions {
    if (json == null) {
        return json;
    }
    return {
        
        'canRead': json['can_read'],
        'canWrite': json['can_write'],
        'canDelete': json['can_delete'],
        'canApprove': json['can_approve'],
    };
}

export function ItemPermissionsToJSON(json: any): ItemPermissions {
    return ItemPermissionsToJSONTyped(json, false);
}

export function ItemPermissionsToJSONTyped(value?: ItemPermissions | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'can_read': value['canRead'],
        'can_write': value['canWrite'],
        'can_delete': value['canDelete'],
        'can_approve': value['canApprove'],
    };
}

