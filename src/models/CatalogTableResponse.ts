/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { CatalogColumnResponse } from './CatalogColumnResponse';
import {
    CatalogColumnResponseFromJSON,
    CatalogColumnResponseFromJSONTyped,
    CatalogColumnResponseToJSON,
    CatalogColumnResponseToJSONTyped,
} from './CatalogColumnResponse';

/**
 * 
 * @export
 * @interface CatalogTableResponse
 */
export interface CatalogTableResponse {
    /**
     * 
     * @type {string}
     * @memberof CatalogTableResponse
     */
    name: string;
    /**
     * 
     * @type {string}
     * @memberof CatalogTableResponse
     */
    schemaName: string;
    /**
     * 
     * @type {boolean}
     * @memberof CatalogTableResponse
     */
    isView?: boolean;
    /**
     * 
     * @type {Array<CatalogColumnResponse>}
     * @memberof CatalogTableResponse
     */
    columns: Array<CatalogColumnResponse>;
}
export const CatalogTableResponsePropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the CatalogTableResponse interface.
 */
export function instanceOfCatalogTableResponse(value: object): value is CatalogTableResponse {
    if (!('name' in value) || value['name'] === undefined) return false;
    if (!('schemaName' in value) || value['schemaName'] === undefined) return false;
    if (!('columns' in value) || value['columns'] === undefined) return false;
    return true;
}

export function CatalogTableResponseFromJSON(json: any): CatalogTableResponse {
    return CatalogTableResponseFromJSONTyped(json, false);
}

export function CatalogTableResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): CatalogTableResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'],
        'schemaName': json['schema_name'],
        'isView': json['is_view'] == null ? undefined : json['is_view'],
        'columns': ((json['columns'] as Array<any>).map(CatalogColumnResponseFromJSON)),
    };
}

export function CatalogTableResponseToJSON(json: any): CatalogTableResponse {
    return CatalogTableResponseToJSONTyped(json, false);
}

export function CatalogTableResponseToJSONTyped(value?: CatalogTableResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'schema_name': value['schemaName'],
        'is_view': value['isView'],
        'columns': ((value['columns'] as Array<any>).map(CatalogColumnResponseToJSON)),
    };
}

