/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { StructuredChangeType } from './StructuredChangeType';
import {
    StructuredChangeTypeFromJSON,
    StructuredChangeTypeFromJSONTyped,
    StructuredChangeTypeToJSON,
    StructuredChangeTypeToJSONTyped,
} from './StructuredChangeType';

/**
 * One changed key-path in a JSON/YAML document.
 * 
 * ``path`` is a dotted/indexed locator like ``services.web.image`` or
 * ``items[3].qty``. ``old`` is null for added, ``new`` is null for removed.
 * @export
 * @interface StructuredChange
 */
export interface StructuredChange {
    /**
     * 
     * @type {string}
     * @memberof StructuredChange
     */
    path: string;
    /**
     * 
     * @type {string}
     * @memberof StructuredChange
     */
    old: string | null;
    /**
     * 
     * @type {string}
     * @memberof StructuredChange
     */
    _new: string | null;
    /**
     * 
     * @type {StructuredChangeType}
     * @memberof StructuredChange
     */
    type: StructuredChangeType;
}


export const StructuredChangePropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the StructuredChange interface.
 */
export function instanceOfStructuredChange(value: object): value is StructuredChange {
    if (!('path' in value) || value['path'] === undefined) return false;
    if (!('old' in value) || value['old'] === undefined) return false;
    if (!('_new' in value) || value['_new'] === undefined) return false;
    if (!('type' in value) || value['type'] === undefined) return false;
    return true;
}

export function StructuredChangeFromJSON(json: any): StructuredChange {
    return StructuredChangeFromJSONTyped(json, false);
}

export function StructuredChangeFromJSONTyped(json: any, ignoreDiscriminator: boolean): StructuredChange {
    if (json == null) {
        return json;
    }
    return {
        
        'path': json['path'],
        'old': json['old'],
        '_new': json['new'],
        'type': StructuredChangeTypeFromJSON(json['type']),
    };
}

export function StructuredChangeToJSON(json: any): StructuredChange {
    return StructuredChangeToJSONTyped(json, false);
}

export function StructuredChangeToJSONTyped(value?: StructuredChange | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'path': value['path'],
        'old': value['old'],
        'new': value['_new'],
        'type': StructuredChangeTypeToJSON(value['type']),
    };
}

