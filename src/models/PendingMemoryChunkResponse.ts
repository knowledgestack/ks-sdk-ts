/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { MemoryScope } from './MemoryScope';
import {
    MemoryScopeFromJSON,
    MemoryScopeFromJSONTyped,
    MemoryScopeToJSON,
    MemoryScopeToJSONTyped,
} from './MemoryScope';
import type { MemoryKind } from './MemoryKind';
import {
    MemoryKindFromJSON,
    MemoryKindFromJSONTyped,
    MemoryKindToJSON,
    MemoryKindToJSONTyped,
} from './MemoryKind';

/**
 * 
 * @export
 * @interface PendingMemoryChunkResponse
 */
export interface PendingMemoryChunkResponse {
    /**
     * 
     * @type {string}
     * @memberof PendingMemoryChunkResponse
     */
    chunkId: string;
    /**
     * 
     * @type {MemoryKind}
     * @memberof PendingMemoryChunkResponse
     */
    kind: MemoryKind;
    /**
     * 
     * @type {MemoryScope}
     * @memberof PendingMemoryChunkResponse
     */
    scope: MemoryScope;
    /**
     * 
     * @type {string}
     * @memberof PendingMemoryChunkResponse
     */
    ownerPathPartId: string;
    /**
     * 
     * @type {string}
     * @memberof PendingMemoryChunkResponse
     */
    proposerTenantUserId: string;
    /**
     * 
     * @type {string}
     * @memberof PendingMemoryChunkResponse
     */
    runId: string | null;
    /**
     * 
     * @type {string}
     * @memberof PendingMemoryChunkResponse
     */
    rationale: string;
    /**
     * 
     * @type {string}
     * @memberof PendingMemoryChunkResponse
     */
    body: string;
}


export const PendingMemoryChunkResponsePropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the PendingMemoryChunkResponse interface.
 */
export function instanceOfPendingMemoryChunkResponse(value: object): value is PendingMemoryChunkResponse {
    if (!('chunkId' in value) || value['chunkId'] === undefined) return false;
    if (!('kind' in value) || value['kind'] === undefined) return false;
    if (!('scope' in value) || value['scope'] === undefined) return false;
    if (!('ownerPathPartId' in value) || value['ownerPathPartId'] === undefined) return false;
    if (!('proposerTenantUserId' in value) || value['proposerTenantUserId'] === undefined) return false;
    if (!('runId' in value) || value['runId'] === undefined) return false;
    if (!('rationale' in value) || value['rationale'] === undefined) return false;
    if (!('body' in value) || value['body'] === undefined) return false;
    return true;
}

export function PendingMemoryChunkResponseFromJSON(json: any): PendingMemoryChunkResponse {
    return PendingMemoryChunkResponseFromJSONTyped(json, false);
}

export function PendingMemoryChunkResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): PendingMemoryChunkResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'chunkId': json['chunk_id'],
        'kind': MemoryKindFromJSON(json['kind']),
        'scope': MemoryScopeFromJSON(json['scope']),
        'ownerPathPartId': json['owner_path_part_id'],
        'proposerTenantUserId': json['proposer_tenant_user_id'],
        'runId': json['run_id'],
        'rationale': json['rationale'],
        'body': json['body'],
    };
}

export function PendingMemoryChunkResponseToJSON(json: any): PendingMemoryChunkResponse {
    return PendingMemoryChunkResponseToJSONTyped(json, false);
}

export function PendingMemoryChunkResponseToJSONTyped(value?: PendingMemoryChunkResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'chunk_id': value['chunkId'],
        'kind': MemoryKindToJSON(value['kind']),
        'scope': MemoryScopeToJSON(value['scope']),
        'owner_path_part_id': value['ownerPathPartId'],
        'proposer_tenant_user_id': value['proposerTenantUserId'],
        'run_id': value['runId'],
        'rationale': value['rationale'],
        'body': value['body'],
    };
}

