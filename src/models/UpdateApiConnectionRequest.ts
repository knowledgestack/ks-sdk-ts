/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { NetworkClass } from './NetworkClass';
import {
    NetworkClassFromJSON,
    NetworkClassFromJSONTyped,
    NetworkClassToJSON,
    NetworkClassToJSONTyped,
} from './NetworkClass';
import type { ApiAuthConfig } from './ApiAuthConfig';
import {
    ApiAuthConfigFromJSON,
    ApiAuthConfigFromJSONTyped,
    ApiAuthConfigToJSON,
    ApiAuthConfigToJSONTyped,
} from './ApiAuthConfig';

/**
 * Partial update (PATCH). A risk-increasing change re-arms the disclaimer.
 * 
 * ``name`` renames the connection and ``parent_path_part_id`` moves it under
 * a new FOLDER; neither is a risk-increasing change, so a rename/move alone
 * leaves the disclaimer intact.
 * @export
 * @interface UpdateApiConnectionRequest
 */
export interface UpdateApiConnectionRequest {
    /**
     * 
     * @type {string}
     * @memberof UpdateApiConnectionRequest
     */
    name?: string | null;
    /**
     * New parent FOLDER path_part to move the connection under.
     * @type {string}
     * @memberof UpdateApiConnectionRequest
     */
    parentPathPartId?: string | null;
    /**
     * 
     * @type {string}
     * @memberof UpdateApiConnectionRequest
     */
    baseUrl?: string | null;
    /**
     * 
     * @type {NetworkClass}
     * @memberof UpdateApiConnectionRequest
     */
    networkClass?: NetworkClass;
    /**
     * 
     * @type {boolean}
     * @memberof UpdateApiConnectionRequest
     */
    verifyTls?: boolean | null;
    /**
     * 
     * @type {ApiAuthConfig}
     * @memberof UpdateApiConnectionRequest
     */
    authConfig?: ApiAuthConfig | null;
    /**
     * 
     * @type {string}
     * @memberof UpdateApiConnectionRequest
     */
    apiDocs?: string | null;
}


export const UpdateApiConnectionRequestPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
    name: {
        maxLength: 255,
        minLength: 1,
    },
    baseUrl: {
        maxLength: 2048,
    },
    apiDocs: {
        maxLength: 100000,
    },
}


/**
 * Check if a given object implements the UpdateApiConnectionRequest interface.
 */
export function instanceOfUpdateApiConnectionRequest(value: object): value is UpdateApiConnectionRequest {
    return true;
}

export function UpdateApiConnectionRequestFromJSON(json: any): UpdateApiConnectionRequest {
    return UpdateApiConnectionRequestFromJSONTyped(json, false);
}

export function UpdateApiConnectionRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): UpdateApiConnectionRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'] == null ? undefined : json['name'],
        'parentPathPartId': json['parent_path_part_id'] == null ? undefined : json['parent_path_part_id'],
        'baseUrl': json['base_url'] == null ? undefined : json['base_url'],
        'networkClass': json['network_class'] == null ? undefined : NetworkClassFromJSON(json['network_class']),
        'verifyTls': json['verify_tls'] == null ? undefined : json['verify_tls'],
        'authConfig': json['auth_config'] == null ? undefined : ApiAuthConfigFromJSON(json['auth_config']),
        'apiDocs': json['api_docs'] == null ? undefined : json['api_docs'],
    };
}

export function UpdateApiConnectionRequestToJSON(json: any): UpdateApiConnectionRequest {
    return UpdateApiConnectionRequestToJSONTyped(json, false);
}

export function UpdateApiConnectionRequestToJSONTyped(value?: UpdateApiConnectionRequest | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'parent_path_part_id': value['parentPathPartId'],
        'base_url': value['baseUrl'],
        'network_class': NetworkClassToJSON(value['networkClass']),
        'verify_tls': value['verifyTls'],
        'auth_config': ApiAuthConfigToJSON(value['authConfig']),
        'api_docs': value['apiDocs'],
    };
}

