/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { DiffRowType } from './DiffRowType';
import {
    DiffRowTypeFromJSON,
    DiffRowTypeFromJSONTyped,
    DiffRowTypeToJSON,
    DiffRowTypeToJSONTyped,
} from './DiffRowType';
import type { DiffSpan } from './DiffSpan';
import {
    DiffSpanFromJSON,
    DiffSpanFromJSONTyped,
    DiffSpanToJSON,
    DiffSpanToJSONTyped,
} from './DiffSpan';

/**
 * One aligned side-by-side row.
 * 
 * ``left`` is the old version, ``right`` is the new. For ``equal`` both are
 * present; ``delete`` has left only; ``insert`` has right only; ``replace``
 * has both plus ``left_spans`` / ``right_spans`` marking the changed words.
 * Line numbers are 1-based and null on the absent side.
 * @export
 * @interface DiffRow
 */
export interface DiffRow {
    /**
     * 
     * @type {DiffRowType}
     * @memberof DiffRow
     */
    type: DiffRowType;
    /**
     * 
     * @type {number}
     * @memberof DiffRow
     */
    leftNo?: number | null;
    /**
     * 
     * @type {string}
     * @memberof DiffRow
     */
    left?: string | null;
    /**
     * 
     * @type {number}
     * @memberof DiffRow
     */
    rightNo?: number | null;
    /**
     * 
     * @type {string}
     * @memberof DiffRow
     */
    right?: string | null;
    /**
     * 
     * @type {Array<DiffSpan>}
     * @memberof DiffRow
     */
    leftSpans?: Array<DiffSpan> | null;
    /**
     * 
     * @type {Array<DiffSpan>}
     * @memberof DiffRow
     */
    rightSpans?: Array<DiffSpan> | null;
}


export const DiffRowPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the DiffRow interface.
 */
export function instanceOfDiffRow(value: object): value is DiffRow {
    if (!('type' in value) || value['type'] === undefined) return false;
    return true;
}

export function DiffRowFromJSON(json: any): DiffRow {
    return DiffRowFromJSONTyped(json, false);
}

export function DiffRowFromJSONTyped(json: any, ignoreDiscriminator: boolean): DiffRow {
    if (json == null) {
        return json;
    }
    return {
        
        'type': DiffRowTypeFromJSON(json['type']),
        'leftNo': json['left_no'] == null ? undefined : json['left_no'],
        'left': json['left'] == null ? undefined : json['left'],
        'rightNo': json['right_no'] == null ? undefined : json['right_no'],
        'right': json['right'] == null ? undefined : json['right'],
        'leftSpans': json['left_spans'] == null ? undefined : ((json['left_spans'] as Array<any>).map(DiffSpanFromJSON)),
        'rightSpans': json['right_spans'] == null ? undefined : ((json['right_spans'] as Array<any>).map(DiffSpanFromJSON)),
    };
}

export function DiffRowToJSON(json: any): DiffRow {
    return DiffRowToJSONTyped(json, false);
}

export function DiffRowToJSONTyped(value?: DiffRow | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'type': DiffRowTypeToJSON(value['type']),
        'left_no': value['leftNo'],
        'left': value['left'],
        'right_no': value['rightNo'],
        'right': value['right'],
        'left_spans': value['leftSpans'] == null ? undefined : ((value['leftSpans'] as Array<any>).map(DiffSpanToJSON)),
        'right_spans': value['rightSpans'] == null ? undefined : ((value['rightSpans'] as Array<any>).map(DiffSpanToJSON)),
    };
}

