/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ConnectionConfig } from './ConnectionConfig';
import {
    ConnectionConfigFromJSON,
    ConnectionConfigFromJSONTyped,
    ConnectionConfigToJSON,
    ConnectionConfigToJSONTyped,
} from './ConnectionConfig';
import type { DataSourceEngine } from './DataSourceEngine';
import {
    DataSourceEngineFromJSON,
    DataSourceEngineFromJSONTyped,
    DataSourceEngineToJSON,
    DataSourceEngineToJSONTyped,
} from './DataSourceEngine';

/**
 * Test a connection without persisting it.
 * 
 * Exactly one mode must be supplied: fresh creds (``engine`` +
 * ``connection_config`` + ``parent_path_part_id``) or a stored connector
 * (``data_source_id``). The handler rejects zero or both modes with a 400.
 * 
 * Fresh mode requires ``parent_path_part_id`` and is gated by write access to
 * that folder — the same permission as creating a connector there. This stops
 * an authenticated user from turning the probe into an arbitrary-host reachability
 * scanner (SSRF) against internal networks.
 * @export
 * @interface TestConnectionRequest
 */
export interface TestConnectionRequest {
    /**
     * Test a stored connector instead of fresh creds
     * @type {string}
     * @memberof TestConnectionRequest
     */
    dataSourceId?: string | null;
    /**
     * 
     * @type {DataSourceEngine}
     * @memberof TestConnectionRequest
     */
    engine?: DataSourceEngine;
    /**
     * 
     * @type {ConnectionConfig}
     * @memberof TestConnectionRequest
     */
    connectionConfig?: ConnectionConfig | null;
    /**
     * Fresh-creds mode: the folder the connector would be created under; the probe is gated by write access to it.
     * @type {string}
     * @memberof TestConnectionRequest
     */
    parentPathPartId?: string | null;
}


export const TestConnectionRequestPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the TestConnectionRequest interface.
 */
export function instanceOfTestConnectionRequest(value: object): value is TestConnectionRequest {
    return true;
}

export function TestConnectionRequestFromJSON(json: any): TestConnectionRequest {
    return TestConnectionRequestFromJSONTyped(json, false);
}

export function TestConnectionRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): TestConnectionRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'dataSourceId': json['data_source_id'] == null ? undefined : json['data_source_id'],
        'engine': json['engine'] == null ? undefined : DataSourceEngineFromJSON(json['engine']),
        'connectionConfig': json['connection_config'] == null ? undefined : ConnectionConfigFromJSON(json['connection_config']),
        'parentPathPartId': json['parent_path_part_id'] == null ? undefined : json['parent_path_part_id'],
    };
}

export function TestConnectionRequestToJSON(json: any): TestConnectionRequest {
    return TestConnectionRequestToJSONTyped(json, false);
}

export function TestConnectionRequestToJSONTyped(value?: TestConnectionRequest | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'data_source_id': value['dataSourceId'],
        'engine': DataSourceEngineToJSON(value['engine']),
        'connection_config': ConnectionConfigToJSON(value['connectionConfig']),
        'parent_path_part_id': value['parentPathPartId'],
    };
}

