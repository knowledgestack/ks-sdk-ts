/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { DiffRow } from './DiffRow';
import {
    DiffRowFromJSON,
    DiffRowFromJSONTyped,
    DiffRowToJSON,
    DiffRowToJSONTyped,
} from './DiffRow';

/**
 * A computed side-by-side diff of two texts.
 * @export
 * @interface TextDiff
 */
export interface TextDiff {
    /**
     * 
     * @type {Array<DiffRow>}
     * @memberof TextDiff
     */
    rows: Array<DiffRow>;
    /**
     * 
     * @type {number}
     * @memberof TextDiff
     */
    added: number;
    /**
     * 
     * @type {number}
     * @memberof TextDiff
     */
    removed: number;
    /**
     * 
     * @type {number}
     * @memberof TextDiff
     */
    changed: number;
    /**
     * 
     * @type {boolean}
     * @memberof TextDiff
     */
    truncated: boolean;
}
export const TextDiffPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the TextDiff interface.
 */
export function instanceOfTextDiff(value: object): value is TextDiff {
    if (!('rows' in value) || value['rows'] === undefined) return false;
    if (!('added' in value) || value['added'] === undefined) return false;
    if (!('removed' in value) || value['removed'] === undefined) return false;
    if (!('changed' in value) || value['changed'] === undefined) return false;
    if (!('truncated' in value) || value['truncated'] === undefined) return false;
    return true;
}

export function TextDiffFromJSON(json: any): TextDiff {
    return TextDiffFromJSONTyped(json, false);
}

export function TextDiffFromJSONTyped(json: any, ignoreDiscriminator: boolean): TextDiff {
    if (json == null) {
        return json;
    }
    return {
        
        'rows': ((json['rows'] as Array<any>).map(DiffRowFromJSON)),
        'added': json['added'],
        'removed': json['removed'],
        'changed': json['changed'],
        'truncated': json['truncated'],
    };
}

export function TextDiffToJSON(json: any): TextDiff {
    return TextDiffToJSONTyped(json, false);
}

export function TextDiffToJSONTyped(value?: TextDiff | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'rows': ((value['rows'] as Array<any>).map(DiffRowToJSON)),
        'added': value['added'],
        'removed': value['removed'],
        'changed': value['changed'],
        'truncated': value['truncated'],
    };
}

