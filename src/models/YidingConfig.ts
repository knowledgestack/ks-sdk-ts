/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * What a customer supplies for a YiDing sync: the summary plus the secret.
 * @export
 * @interface YidingConfig
 */
export interface YidingConfig {
    /**
     * The account the crawler signs in to the panel as
     * @type {string}
     * @memberof YidingConfig
     */
    username: string;
    /**
     * The panel's own id for the shop. Only the customer list is scoped by it — orders follow the logged-in account — but it answers a wrong one with a 500, so an empty value fails the crawl, not just that page.
     * @type {string}
     * @memberof YidingConfig
     */
    businessId: string;
    /**
     * First dining day to sync; earlier orders are not fetched
     * @type {Date}
     * @memberof YidingConfig
     */
    startDate: Date;
    /**
     * Crawl recurrence, in the tenant's timezone
     * @type {string}
     * @memberof YidingConfig
     */
    cron: string;
    /**
     * Root of the YiDing admin panel the crawler signs in to
     * @type {string}
     * @memberof YidingConfig
     */
    baseUrl?: string;
    /**
     * Write-only: the settings panel never reads it back
     * @type {string}
     * @memberof YidingConfig
     */
    password: string;
}
export const YidingConfigPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
    username: {
        minLength: 1,
    },
    businessId: {
        minLength: 1,
    },
    cron: {
        minLength: 1,
    },
    baseUrl: {
        maxLength: 2048,
    },
    password: {
        minLength: 1,
    },
}


/**
 * Check if a given object implements the YidingConfig interface.
 */
export function instanceOfYidingConfig(value: object): value is YidingConfig {
    if (!('username' in value) || value['username'] === undefined) return false;
    if (!('businessId' in value) || value['businessId'] === undefined) return false;
    if (!('startDate' in value) || value['startDate'] === undefined) return false;
    if (!('cron' in value) || value['cron'] === undefined) return false;
    if (!('password' in value) || value['password'] === undefined) return false;
    return true;
}

export function YidingConfigFromJSON(json: any): YidingConfig {
    return YidingConfigFromJSONTyped(json, false);
}

export function YidingConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): YidingConfig {
    if (json == null) {
        return json;
    }
    return {
        
        'username': json['username'],
        'businessId': json['business_id'],
        'startDate': (new Date(json['start_date'])),
        'cron': json['cron'],
        'baseUrl': json['base_url'] == null ? undefined : json['base_url'],
        'password': json['password'],
    };
}

export function YidingConfigToJSON(json: any): YidingConfig {
    return YidingConfigToJSONTyped(json, false);
}

export function YidingConfigToJSONTyped(value?: YidingConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'username': value['username'],
        'business_id': value['businessId'],
        'start_date': value['startDate'].toISOString().substring(0,10),
        'cron': value['cron'],
        'base_url': value['baseUrl'],
        'password': value['password'],
    };
}

