/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ColumnReference } from './ColumnReference';
import {
    ColumnReferenceFromJSON,
    ColumnReferenceFromJSONTyped,
    ColumnReferenceToJSON,
    ColumnReferenceToJSONTyped,
} from './ColumnReference';

/**
 * A modeled column. Only ``exposed`` columns are surfaced to the agent.
 * @export
 * @interface ColumnConfig
 */
export interface ColumnConfig {
    /**
     * 
     * @type {string}
     * @memberof ColumnConfig
     */
    name: string;
    /**
     * 
     * @type {string}
     * @memberof ColumnConfig
     */
    dataType?: string;
    /**
     * 
     * @type {string}
     * @memberof ColumnConfig
     */
    comment?: string | null;
    /**
     * 
     * @type {boolean}
     * @memberof ColumnConfig
     */
    isPk?: boolean;
    /**
     * 
     * @type {boolean}
     * @memberof ColumnConfig
     */
    exposed?: boolean;
    /**
     * 
     * @type {ColumnReference}
     * @memberof ColumnConfig
     */
    references?: ColumnReference | null;
}
export const ColumnConfigPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the ColumnConfig interface.
 */
export function instanceOfColumnConfig(value: object): value is ColumnConfig {
    if (!('name' in value) || value['name'] === undefined) return false;
    return true;
}

export function ColumnConfigFromJSON(json: any): ColumnConfig {
    return ColumnConfigFromJSONTyped(json, false);
}

export function ColumnConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): ColumnConfig {
    if (json == null) {
        return json;
    }
    return {
        
        'name': json['name'],
        'dataType': json['data_type'] == null ? undefined : json['data_type'],
        'comment': json['comment'] == null ? undefined : json['comment'],
        'isPk': json['is_pk'] == null ? undefined : json['is_pk'],
        'exposed': json['exposed'] == null ? undefined : json['exposed'],
        'references': json['references'] == null ? undefined : ColumnReferenceFromJSON(json['references']),
    };
}

export function ColumnConfigToJSON(json: any): ColumnConfig {
    return ColumnConfigToJSONTyped(json, false);
}

export function ColumnConfigToJSONTyped(value?: ColumnConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'name': value['name'],
        'data_type': value['dataType'],
        'comment': value['comment'],
        'is_pk': value['isPk'],
        'exposed': value['exposed'],
        'references': ColumnReferenceToJSON(value['references']),
    };
}

