/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { ColumnConfig } from './ColumnConfig';
import {
    ColumnConfigFromJSON,
    ColumnConfigFromJSONTyped,
    ColumnConfigToJSON,
    ColumnConfigToJSONTyped,
} from './ColumnConfig';

/**
 * Model a DB table as a queryable child of the connector.
 * @export
 * @interface ModelTableRequest
 */
export interface ModelTableRequest {
    /**
     * 
     * @type {string}
     * @memberof ModelTableRequest
     */
    tableName: string;
    /**
     * Schema/namespace of the table; omit for the default schema
     * @type {string}
     * @memberof ModelTableRequest
     */
    schemaName?: string | null;
    /**
     * Display name; defaults to table_name
     * @type {string}
     * @memberof ModelTableRequest
     */
    name?: string | null;
    /**
     * Omit to auto-introspect all columns as exposed
     * @type {Array<ColumnConfig>}
     * @memberof ModelTableRequest
     */
    columnConfig?: Array<ColumnConfig> | null;
}
export const ModelTableRequestPropertyValidationAttributesMap: {
    [property: string]: {
        maxLength?: number,
        minLength?: number,
        pattern?: string,
        maximum?: number,
        exclusiveMaximum?: boolean,
        minimum?: number,
        exclusiveMinimum?: boolean,
        multipleOf?: number,
        maxItems?: number,
        minItems?: number,
        uniqueItems?: boolean
    }
} = {
}


/**
 * Check if a given object implements the ModelTableRequest interface.
 */
export function instanceOfModelTableRequest(value: object): value is ModelTableRequest {
    if (!('tableName' in value) || value['tableName'] === undefined) return false;
    return true;
}

export function ModelTableRequestFromJSON(json: any): ModelTableRequest {
    return ModelTableRequestFromJSONTyped(json, false);
}

export function ModelTableRequestFromJSONTyped(json: any, ignoreDiscriminator: boolean): ModelTableRequest {
    if (json == null) {
        return json;
    }
    return {
        
        'tableName': json['table_name'],
        'schemaName': json['schema_name'] == null ? undefined : json['schema_name'],
        'name': json['name'] == null ? undefined : json['name'],
        'columnConfig': json['column_config'] == null ? undefined : ((json['column_config'] as Array<any>).map(ColumnConfigFromJSON)),
    };
}

export function ModelTableRequestToJSON(json: any): ModelTableRequest {
    return ModelTableRequestToJSONTyped(json, false);
}

export function ModelTableRequestToJSONTyped(value?: ModelTableRequest | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'table_name': value['tableName'],
        'schema_name': value['schemaName'],
        'name': value['name'],
        'column_config': value['columnConfig'] == null ? undefined : ((value['columnConfig'] as Array<any>).map(ColumnConfigToJSON)),
    };
}

