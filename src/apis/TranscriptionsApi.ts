/* tslint:disable */
/* eslint-disable */
/**
 * Knowledge Stack API
 * Knowledge Stack backend API for authentication and knowledge management.  ## Integrating (RPA / machine clients)  **Base URL.** Knowledge Stack is self-hosted — point at your own deployment host (see `servers`). The `localhost` entry is for local development only.  **Authentication.** Send `Authorization: Bearer <api-key>` on every request. Mint an API key once via `POST /v1/api-keys` from a signed-in browser session; the raw `sk-user-...` secret is returned **only** at creation, so store it then. A key inherits its owning user\'s live tenant role and path permissions — create RPA keys from a least-privilege user, and set `expires_at` for rotation. The `ks_uat` cookie scheme is browser-only and cannot be used by headless clients.  **Async work is polled, not pushed.** There are no outbound webhooks. - `POST /v1/documents/ingest` returns `201` immediately with a `workflow_id`;   poll `GET /v1/system-jobs/document_versions/{workflow_id}` until `status` is   terminal (anything other than `pending`/`processing`). The `Location` response   header points at this poll resource. - `POST /v1/workflow-runs/{run_id}/start` returns `202`; poll   `GET /v1/workflow-runs/{run_id}` until `execution_state` is `COMPLETED` or   `FAILED`. The `Location` header points at the run resource. - `POST /v1/agent/ask` is **synchronous** — it blocks until the agent finishes   and returns the answer inline. Use a generous HTTP timeout.  **Pagination.** List endpoints accept `limit`/`offset` and return `{items, total, limit, offset}`.  **Errors.** Every non-2xx body is `{detail, code, request_id}`. `code` is a stable value from a closed set (see the `ErrorResponse` schema\'s `code` enum) — branch on it rather than parsing `detail`. Quota rejections return `429` with a `Retry-After` header; transient lock contention returns a retryable `503`. Quote `request_id` (also the `x-request-id` response header) to support.  **Idempotency.** `POST /v1/workflow-runs` accepts an `idempotency_key` to dedupe retried run creation. `agent/ask` charges one message *before* running and does not refund a client-cancelled call. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  HTTPValidationError,
  SupportedLanguage,
  TranscriptionResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
    SupportedLanguageFromJSON,
    SupportedLanguageToJSON,
    TranscriptionResponseFromJSON,
    TranscriptionResponseToJSON,
} from '../models/index';

export interface CreateTranscriptionRequest {
    file: Blob;
    language?: SupportedLanguage;
}

/**
 * TranscriptionsApi - interface
 * 
 * @export
 * @interface TranscriptionsApiInterface
 */
export interface TranscriptionsApiInterface {
    /**
     * Creates request options for createTranscription without sending the request
     * @param {Blob} file The audio clip. Accepts what a browser records (audio/webm, audio/ogg, audio/mp4) plus audio/mpeg, audio/wav and audio/flac. Re-encoded server-side, so the container does not have to match the backend.
     * @param {SupportedLanguage} [language] 
     * @throws {RequiredError}
     * @memberof TranscriptionsApiInterface
     */
    createTranscriptionRequestOpts(requestParameters: CreateTranscriptionRequest): Promise<runtime.RequestOpts>;

    /**
     * Transcribe a short spoken clip to text.  **Answers synchronously.** Unlike document ingestion there is no `Location` header and nothing to poll — the transcript is in the response body. Expect roughly a second per 10s of audio.  Send `multipart/form-data` with a `file` part; add `language` only to pin `zh` or `en` instead of auto-detecting. The clip is re-encoded server-side to mono 16 kHz mp3, so any listed container works.  Limits: **25 MiB** and **5 min** per clip.  Errors, all carrying the standard `code`/`request_id` body: `413` clip over either limit · `415` content type not accepted · `422` empty clip, or audio that could not be decoded · `503` the speech backend is unreachable or unconfigured.  The audio is held for one ASR call and never persisted; the transcript is never logged.
     * @summary Create Transcription Handler
     * @param {Blob} file The audio clip. Accepts what a browser records (audio/webm, audio/ogg, audio/mp4) plus audio/mpeg, audio/wav and audio/flac. Re-encoded server-side, so the container does not have to match the backend.
     * @param {SupportedLanguage} [language] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof TranscriptionsApiInterface
     */
    createTranscriptionRaw(requestParameters: CreateTranscriptionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TranscriptionResponse>>;

    /**
     * Transcribe a short spoken clip to text.  **Answers synchronously.** Unlike document ingestion there is no `Location` header and nothing to poll — the transcript is in the response body. Expect roughly a second per 10s of audio.  Send `multipart/form-data` with a `file` part; add `language` only to pin `zh` or `en` instead of auto-detecting. The clip is re-encoded server-side to mono 16 kHz mp3, so any listed container works.  Limits: **25 MiB** and **5 min** per clip.  Errors, all carrying the standard `code`/`request_id` body: `413` clip over either limit · `415` content type not accepted · `422` empty clip, or audio that could not be decoded · `503` the speech backend is unreachable or unconfigured.  The audio is held for one ASR call and never persisted; the transcript is never logged.
     * Create Transcription Handler
     */
    createTranscription(requestParameters: CreateTranscriptionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TranscriptionResponse>;

}

/**
 * 
 */
export class TranscriptionsApi extends runtime.BaseAPI implements TranscriptionsApiInterface {

    /**
     * Creates request options for createTranscription without sending the request
     */
    async createTranscriptionRequestOpts(requestParameters: CreateTranscriptionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['file'] == null) {
            throw new runtime.RequiredError(
                'file',
                'Required parameter "file" was null or undefined when calling createTranscription().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }

        if (requestParameters['language'] != null) {
            formParams.append('language', requestParameters['language'] as any);
        }


        let urlPath = `/v1/transcriptions`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Transcribe a short spoken clip to text.  **Answers synchronously.** Unlike document ingestion there is no `Location` header and nothing to poll — the transcript is in the response body. Expect roughly a second per 10s of audio.  Send `multipart/form-data` with a `file` part; add `language` only to pin `zh` or `en` instead of auto-detecting. The clip is re-encoded server-side to mono 16 kHz mp3, so any listed container works.  Limits: **25 MiB** and **5 min** per clip.  Errors, all carrying the standard `code`/`request_id` body: `413` clip over either limit · `415` content type not accepted · `422` empty clip, or audio that could not be decoded · `503` the speech backend is unreachable or unconfigured.  The audio is held for one ASR call and never persisted; the transcript is never logged.
     * Create Transcription Handler
     */
    async createTranscriptionRaw(requestParameters: CreateTranscriptionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TranscriptionResponse>> {
        const requestOptions = await this.createTranscriptionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TranscriptionResponseFromJSON(jsonValue));
    }

    /**
     * Transcribe a short spoken clip to text.  **Answers synchronously.** Unlike document ingestion there is no `Location` header and nothing to poll — the transcript is in the response body. Expect roughly a second per 10s of audio.  Send `multipart/form-data` with a `file` part; add `language` only to pin `zh` or `en` instead of auto-detecting. The clip is re-encoded server-side to mono 16 kHz mp3, so any listed container works.  Limits: **25 MiB** and **5 min** per clip.  Errors, all carrying the standard `code`/`request_id` body: `413` clip over either limit · `415` content type not accepted · `422` empty clip, or audio that could not be decoded · `503` the speech backend is unreachable or unconfigured.  The audio is held for one ASR call and never persisted; the transcript is never logged.
     * Create Transcription Handler
     */
    async createTranscription(requestParameters: CreateTranscriptionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TranscriptionResponse> {
        const response = await this.createTranscriptionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
